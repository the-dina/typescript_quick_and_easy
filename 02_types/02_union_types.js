var something;
something = [1, 2, 3];
something = "one string";
var theLentgh = something.length;
if (typeof something === "string") {
    something.substr(1, 3);
}
something.substr(1, 3);
something.substr(1, 3);
